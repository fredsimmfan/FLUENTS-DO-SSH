# gitcon 

# denizen teletype

ssh-keygen

# enter the name for your public and private keys 

# public and private keys are generated in the current working directory where you've called the "ssh-keygen" command

# click on the "makeshift.pub" file and copy the contents inside it to clipboard by hitting "control + a" and "control + c"

# makeshift is the public key file

# log into Digital Ocean. On the left sidebar, under "Account" click on "Security". 

# Then, click on "add SSH key" button and paste the public key generated earlier. 

# Hit the save button giving an appropriate name for the ssh  


